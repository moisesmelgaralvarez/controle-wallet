{
  "name": "controle-wallet",
  "version": "0.1.0",
  "private": true,
  "description": "Controle Wallet — finanzas personales del hogar",
  "type": "module",
  "scripts": {
    "pruebas": "node --test \"pruebas/**/*.prueba.js\"",
    "pruebas:ver": "node --test --watch \"pruebas/**/*.prueba.js\"",
    "publicar": "wrangler deploy --env=\"\"",
    "publicar:pruebas": "wrangler deploy --env pruebas",
    "vista": "wrangler dev"
  },
  "devDependencies": {
    "supabase": "^2.113.0",
    "wrangler": "^4.120.0"
  },
  "engines": {
    "node": ">=24"
  }
}
